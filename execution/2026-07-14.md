今天的话，主主线任务主要是做“课程云”，最近一段时间都在构建它。目前还在 0.0.x 版本（0.1 版本之前）进行构建工作。

下一步的主要计划如下：
1. 数据模型与命令行开发：先把课程、课堂和考核三个场景的主要数据模型写出来，然后把命令行做出来。
2. 数据规范化：用命令行在案例库中生成对应数据，将原本文字版的案例规范化为数据。
3. 系统与服务构建：按照类似的格式想办法把系统做出来，然后做集成测试，接着逐步把服务客户端和服务端做出来。

利用一两天的时间构建出来之后，其实还是不知道做什么，因为感觉让它去支持 Web Coding 的课程，还是稍微有那么一些难搞。

我觉得可能有一些需要跟 AI 讨论的地方。这就是典型的场景：搞不清楚该不该干、该怎么干的场景。